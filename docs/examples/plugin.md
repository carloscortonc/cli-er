# Example: Plugin

Source: [`examples/plugin/`](https://github.com/carloscortonc/cli-er/tree/develop/examples/plugin)

Demonstrates how to bundle hooks into a reusable `Plugin` object via `CliOptions.plugins`.

See [Features → Plugins](/guide/features#plugins) for the plugin interface and execution-order rules.
