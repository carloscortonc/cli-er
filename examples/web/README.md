Example web application utilizing [`browserify`](https://github.com/browserify/browserify)

To run the project:

```sh
# bundle the script
$ npm run bundle

# start the application (replace "bunx" with "npx" if not installed )
$ npm run start
```